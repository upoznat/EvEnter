<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.eventer.eventticket.dao.mapper.CustomerMapper">

    <resultMap type="Customer" id="customer">
        <result property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="identityNumber" column="identity_number"/>
        <result property="firstName" column="first_name"/>
        <result property="lastName" column="last_name"/>
        <result property="address" column="address"/>
        <result property="registrationDate" column="registration_date"/>
        <result property="email" column="email"/>
        <result property="status" column="status"/>
    </resultMap>


    <insert id="saveCustomer" parameterType="Customer" useGeneratedKeys="true" keyColumn="id" keyProperty="id">
        insert into customer
            (id, username, password, identity_number, first_name, last_name, address, registration_date, email, status)
        values (seq_customers.nextval, #{username}, #{password}, #{identityNumber}, #{firstName}, #{lastName}, #{address}, #{registrationDate}, #{email}, #{status})
    </insert>

    <select id="findCustomer" parameterType="Long" resultMap="customer">
        select *
        from customer
        where id = #{id}
    </select>

    <select id="findCustomerByUsernameOrEmail" parameterType="String" resultMap="customer">
        select *
        from customer
        where username = #{username} or email = #{email}
    </select>

    <select id="findCustomerByIdentityNumber" parameterType="String" resultMap="customer">
        select *
        from customer
        where identity_number = #{identityNumber}
    </select>

    <select id="findCustomerByUsername" parameterType="String" resultMap="customer">
        select *
        from customer
        where username = #{username}
    </select>

    <update id="updateCustomer" parameterType="Customer">
        update customer
        set username = #{username},
            identity_number = #{identityNumber},
            password = #{password},
            first_name = #{firstName},
            last_name = #{lastName},
            address  = #{address},
            registration_date = #{registrationDate},
            email = #{email},
            status  = #{status}
        where id = #{id}
    </update>

</mapper>